services:
  gov-mcp-server:
    build: .
    container_name: gov-mcp-server
    ports:
      - "8000:8000"
    environment:
      - MCP_TRANSPORT=http
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
